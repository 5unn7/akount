node_modules
.next
.turbo
dist
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.seed
**/.turbo/
nul

# Secrets/tokens
figmatoken

# TypeScript build info
*.tsbuildinfo

# Review agent output (temporary)
.reviews/

# Task claims (machine-local, not shared via git)
.claude/task-claims.json

# Task ID counters (machine-local, atomic ID generation)
.claude/.task-id-counters.json
.claude/.task-id-counters.json.lock
.claude/.task-id-counters.json.tmp

# Local settings (may contain API keys)
.claude/settings.local.json

# Runtime logs (dev-only, machine-local)
.claude/runtime/
