---
description: execute implementation plans systematically
---

1. Read the implementation plan
Locate the relevant document in `docs/plans/`.
2. Break down into atomic tasks
Verify the task list in `TASKS.md` or equivalent.
3. Systematical implementation
Write code and tests as defined in the plan.
4. Continuous validation
Run tests frequently and verify after each atomic step.
5. Update task progress
Mark tasks as completed in `task.md` or `TASKS.md`.
