/**
 * Re-export shared AI types from @akount/types.
 * Local consumers continue importing from this file for backward compatibility.
 */
export type {
  AIMessageRole,
  AIMessage,
  AIChatOptions,
  AIChatResponse,
  AIProvider,
} from '@akount/types';
