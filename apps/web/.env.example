# =============================================================================
# Akount Web — Environment Variables
# =============================================================================
# Rename this file to .env.local and fill in the values.
# NEVER commit .env.local files — they may contain secrets.
# =============================================================================

# --- Required ---

# Clerk authentication — get from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Clerk webhook secret — for user sync webhook
# Get from Clerk Dashboard > Webhooks > Signing Secret
CLERK_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Optional (have sensible defaults) ---

# API server URL (defaults to http://localhost:4000)
NEXT_PUBLIC_API_URL=http://localhost:4000

# =============================================================================
# Production Checklist:
# 1. All "Required" vars MUST be set
# 2. NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY must be production key (pk_live_*)
# 3. CLERK_SECRET_KEY must be production key (sk_live_*)
# 4. NEXT_PUBLIC_API_URL must point to production API
# 5. Store secrets in your platform's vault (Vercel env vars, etc.)
#    — NEVER use .env files in production
# =============================================================================
